footer.py-3.mt-3
  .container.text-center
    a(href=config.urls.web)
      != png2x({ text: config.appName, fontSize: 49, fontNameOrPath: 'Bitter Bold' })
    p
      small.text-muted
        = '©'
        = moment().format('YYYY')
        = ' '
        = config.appName
        = '. '
        = t('All Rights Reserved.')
        = ' '
        a(href=`${config.urls.web}/terms`)= t('Terms')
